# Phase 2.1d `content_demand` Checkpoint - 2026-05-14

> **Purpose:** Self-contained handoff. Phase 2.1d build is complete; the Dale-led quality gate (3-domain smoke) is still pending. Once it signs off, `feat/phase-2.1d-content-demand` merges to `main` and Section A is closed (4 of 4 collectors shipped).

**Phase status:** Build complete; quality gate (manual, Dale-led) pending
**Branch:** `feat/phase-2.1d-content-demand` (worktree off `main`)
**Last commit (build):** `218cf73` (T14 pipeline registration)
**Last commit (closeout):** `f2b351e` (plan T7 adaptations backport - see "Process notes")
**Test status:** 466 passed, 1 skipped, ruff clean
**Total elapsed:** ~2.5h subagent execution across 14 tasks

---

## What this phase shipped

Phase 2.1d is the fourth Section A collector and is structurally identical to Phase 2.1c (same 14-task shape: schemas, catalog, collector skeleton, parsers + detectors, findings, evidence, orchestration, synthesizer wiring, renderer subsection, pipeline registration). It completes the four-signal Section A picture: pricing + tech_stack + revenue_motion + content_demand.

**Deliverables:**

- `content_demand` collector - scrapes the homepage + a discovered blog index, surfaces four content surfaces: blog cadence + post-mix categorization, lead-magnet posture, podcast presence, newsletter shape (Substack vs embedded form vs none)
- `ContentDemandData`, `BlogPost`, `LeadMagnet` schemas at `rrxray/schemas/content_demand.py`
- `_content_demand_catalog.py` - 8 content categories (seo_listicle, thought_leadership, founder_essay, customer_story, product_update, how_to, research, other) + 7 lead-magnet asset types (ebook, whitepaper, report, guide, template, calculator, checklist) + 2 podcast platforms (Apple, Spotify) + Substack regex
- Section A synthesizer (`rrxray/synthesizers/observed_gtm_motion.py` + `rrxray/prompts/observed_gtm_motion.md`) now reads four signals - added a fourth conditional block + framework guidance for content-demand posture interpretation
- Renderer Module Detail Appendix subsection (`templates/_content_demand_detail.md.jinja` + include in `templates/report_internal.md.jinja`)
- 73 new tests; suite grew 393 → 466 passed
- Pipeline registration in `rrxray/pipeline.py` `COLLECTORS` list

---

## Where the work lives

- **Spec:** `docs/superpowers/specs/2026-05-08-rrxray-phase-2.1d-content-demand-design.md` (refreshed 2026-05-13)
- **Plan:** `docs/superpowers/plans/2026-05-13-rrxray-phase-2.1d-content-demand.md` (now includes the T7-adaptations note + corrected Step 3 verbatim block, per closeout commit `f2b351e`)

**New / modified files (high-level):**

- New: `rrxray/collectors/content_demand.py`, `rrxray/collectors/_content_demand_catalog.py`, `rrxray/schemas/content_demand.py`, `templates/_content_demand_detail.md.jinja`, `tests/test_content_demand.py`, `tests/test_content_demand_catalog.py`, `tests/test_content_demand_schemas.py`, `tests/fixtures/synthetic/content_demand/*.html`
- Modified: `rrxray/schemas/data.py` (added `content_demand` field on `CollectorOutputs`), `rrxray/pipeline.py` (registered collector), `rrxray/prompts/observed_gtm_motion.md` (fourth conditional block + framework guidance), `rrxray/synthesizers/observed_gtm_motion.py` (reads `content_demand`), `templates/report_internal.md.jinja` (includes new partial)

**Notable commits (chronological):**

- `b25091f` - `ContentDemandData`, `BlogPost`, `LeadMagnet` schemas
- `a411f4b` - `content_demand` field on `CollectorOutputs`
- `d578d86` - content_demand catalog (8 categories + lead-magnet / podcast / newsletter patterns)
- `6a16013` - collector skeleton: blog URL discovery
- `f2d14fa` - blog post parsing with anchor + time-tag extraction
- `4b8af4e` - content categorization: 8-category keyword catalog + SEO listicle regex
- `aef24a8` - lead-magnet detection: CTA patterns + form-near heuristic + asset-type inference (T7; this is the commit that surfaced the two adaptations now backported into the plan)
- `562bc93` - podcast detection: RSS head sniff + Apple/Spotify regex
- `ba1bf0d` - newsletter detection: Substack regex + embedded-form heuristic
- `f09d982` - post-count aggregation + rule-based content_demand findings
- `ed0fa31` - `collect()` orchestration + evidence + graceful error handling
- `d7fa324` - Module Detail Appendix Content Demand subsection
- `7dba4e8` - Section A synthesizer reads `content_demand` (fourth signal)
- `218cf73` - pipeline registration
- `f2b351e` - closeout: plan T7 backport to reflect verbatim-code adaptations

---

## Test status

```
466 passed, 1 skipped in ~1s
```

`PYTHONPATH=. /Users/dalezwizinski/Documents/Apps/rrxray/.venv/bin/python -m ruff check rrxray/ tests/` - clean.

Voice clean per T10 verification (no em-dashes, no forbidden words, → bullets where applicable, GTM Gap™ on first use).

---

## Known issues / limitations

Surfaced during the final whole-branch review. None are crashes; all are deferred concerns or trade-offs.

- **Important - newsletter false-positive risk.** `_detect_newsletter`'s 200-char "heading near form" heuristic can match an unrelated demo-signup or contact form on the homepage (e.g. "Sign up for a demo" near a form with an email input). Currently any such match classifies as `embedded_form`, which the synthesizer reads as "corporate newsletter shape." Spec intent allows newsletter-shaped intent broadly, so this is borderline-acceptable. Decision point at quality gate: tighten the heuristic (require an explicit "newsletter" / "subscribe" keyword rather than the broader "sign up") or accept the current looseness.
- **Important - scattered imports inside collector.** `rrxray/collectors/content_demand.py` has 9 mid-file import sites with blanket `# noqa: I001` / `# noqa: E402` markers. Artifact of building one-import-per-TDD-task. Ruff is clean because of the noqa markers but a future reader will find it ugly. Defer to a housekeeping commit after merge: consolidate to a single import block at the top of the file.
- **Important - author regex looseness.** `_AUTHOR_RE` (`(?:class=["\'][^"\']*author[^"\']*["\']|>By\s+)([^<]+)`) matches `>By\s+` anywhere in HTML and the `class="...author..."` capture group reads until the next `<`, which means a timestamp or other inline element can leak in. The 80-char cap mitigates but does not eliminate. Defer; flag if a live run produces noisy author values.
- **Minor - `_BLOG_LINK_RE` does not capture nested-element titles.** Anchors that wrap an `<h2>` or `<span>` (rather than plain text) get skipped. Acceptable v1 - most blog index pages render plain anchor text.
- **Minor - `LEAD_MAGNET_CTA_PATTERNS` "state of" under `report` is broad.** Could match "state of the union" or similar phrases in a thought-leadership essay. Acceptable but noted.

---

## Environment gotchas

- **`uv` is not available in this sandbox.** The parent venv at `/Users/dalezwizinski/Documents/Apps/rrxray/.venv` is the working setup. Tests run with `PYTHONPATH=. /Users/dalezwizinski/Documents/Apps/rrxray/.venv/bin/pytest`. Same for `python -m rrxray run ...`.
- **`peopledatalabs` package was missing from the parent venv at session start.** Bootstrapped via `ensurepip` + `pip install peopledatalabs`. After install, the baseline pytest count went from 392 → 393 passed (one previously-skipped test was activated). Future sessions on this venv should not hit this again.
- **Worktree `.env` is symlinked from parent project** per the `feedback_worktree_env` memory. Required for any live `rrxray run` invocation.
- The standing list in `CLAUDE.md` covers the rest (empty `ANTHROPIC_API_KEY` shadowing `.env`, `uv run rrxray` `.pth` newline issue on Python 3.14, archive.org 503s).

---

## What's queued next

**Immediate - Dale-led quality gate.** 3-domain smoke against Swayable + Healthicity + Linear (regression check per the rrxray ICP discipline). Run via:

```bash
cd /Users/dalezwizinski/Documents/Apps/rrxray/.claude/worktrees/phase-2.1d-content-demand
PYTHONPATH=. /Users/dalezwizinski/Documents/Apps/rrxray/.venv/bin/python -m rrxray run --domain swayable.com --no-cache 2>&1 | tail -3
```

Iterate the Section A prompt only; do not redesign the collector. Expected: Swayable + Healthicity narratives confidently weave the four signals (pricing + tech_stack + revenue_motion + content_demand). Linear is regression-only per the ICP discipline (small founder-led B2B is the bar; Linear-shaped PLG giants are checked only to confirm nothing regressed).

**After quality gate signs off:** merge `feat/phase-2.1d-content-demand` → `main`. Closes Section A (4 of 4 collectors shipped).

**Beyond Section A:**

- Phase 2.3 (next collector - possibly `customer_concentration` or `positioning_drift`)
- or Phase 2.4 (Section B widening - `funding_trajectory` + `customer_concentration` feeding the existing `observed_stability_trajectory` synthesizer)

Pick at the next session start based on Section A quality-gate findings and roadmap priorities.

---

## Open todos / in-flight work

- Dale-led 3-domain quality gate (above)
- Merge `feat/phase-2.1d-content-demand` → `main` once quality gate passes
- Optional housekeeping after merge: consolidate the scattered imports in `rrxray/collectors/content_demand.py` (see "Known issues")

Nothing else in flight.

---

## Process notes

**Workflow:** 14 tasks executed via `superpowers:subagent-driven-development`, fresh subagent per task, TDD discipline, two-stage review applied selectively (skipped on truly mechanical tasks, run fully on logic tasks).

**Model assignments per CLAUDE.md matrix:**

- Haiku 4.5 (mechanical): T1 (schemas), T2 (field addition on `CollectorOutputs`), T3 (catalog), T12 (Module Detail Appendix subsection), T14 (pipeline registration)
- Opus 4.7 (logic): T4 (collector skeleton + blog URL discovery), T5 (blog post parsing), T6 (categorization), T7 (lead-magnet detection), T8 (podcast detection), T9 (newsletter detection), T10 (post-count aggregation + findings), T11 (`collect()` orchestration + evidence + error handling), T13 (Section A synthesizer wiring)

**Key event: T7 caught two real bugs in the plan's verbatim code** (anchor selection bias + form-gate proximity bleed). The Opus implementer fixed both surgically with transparent flagging in the commit message - the kind of catch Sonnet has historically missed. Plan now updated to match (closeout commit `f2b351e`).

**Discipline issues:**

- T2 had a single issue: the implementer used `git add .` and swept in a stray plan file (an old `2026-05-12-...` draft sitting on disk from the brainstorming session). Fixed by amend. Going forward: every implementer prompt now reminds them to stage files explicitly by name.
- T6 + T10 each had a single `# noqa: RUF059` addition for an unused test variable - transparent and intentional.

**Most commits passed all gates clean** (spec compliance + code quality reviewers returned DONE without concerns).

**Token-budget observations:**

- Opus implementers on T4-T11 + T13 used most of the subagent budget. Haiku reviewers were cheap.
- The two-stage review on logic tasks was worth it: T7 surfaced its bugs through the code-quality reviewer's concerns, which the implementer then addressed in the same task cycle.

**Recommendation for next phase:**

- Continue using the matrix from day one. The Opus-on-logic premium has paid for itself twice now (Phase 2.1c voice-sanitizer catch, Phase 2.1d T7 anchor/form-gate catch).
- Expect at least one voice-related fix during the quality gate; that pattern has held in every Section A phase. Templated fix: extend the sanitizer table or tighten the prompt.

---

## Pointers

- [`CLAUDE.md`](../../CLAUDE.md) - project memory, mandatory checkpoint rule, model-selection matrix
- [`roadmap.md`](../../roadmap.md) - phased roadmap with decisions log
- [Phase 2.1d spec](../superpowers/specs/2026-05-08-rrxray-phase-2.1d-content-demand-design.md) (refreshed 2026-05-13)
- [Phase 2.1d plan](../superpowers/plans/2026-05-13-rrxray-phase-2.1d-content-demand.md) (now includes T7 adaptations note)
- [Phase 2.1c checkpoint](2026-05-08-phase-2.1c-revenue-motion-checkpoint.md) - same-shape prior phase; the playbook this phase followed
- Final review notes from the whole-branch reviewer subagent that just ran - see the "Known issues / limitations" section above for the consolidated list
