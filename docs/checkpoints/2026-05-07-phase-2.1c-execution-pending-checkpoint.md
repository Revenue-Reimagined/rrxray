# Phase 2.1c Execution-Pending Checkpoint — 2026-05-07

> **Purpose:** Session-boundary handoff. Phase 2.1c spec and implementation plan are committed; execution has not started. The next Claude session (or a different user) should read this, then `CLAUDE.md`, then the plan, and pick up at Task 1.

**Phase status:** Phase 2.1c spec + plan committed; **execution pending** (no implementation work has started)
**Branch:** `feat/phase-1-foundation` (still not merged to `main`; carries Phase 1 + 2.1a + 2.1a-fix + 2.1b + Phase 2.1c spec/plan)
**Last commit:** `20d1966` (Phase 2.1c implementation plan)
**Test status:** 188 passed, 1 skipped, ruff clean (state at end of Phase 2.1b iteration)
**Why this checkpoint exists:** Dale flagged session-continuity risk after a long session covering Phase 1 + Phase 2.1a + Phase 2.1a-fix + Phase 2.1b + Phase 2.1c brainstorm/spec/plan. Per `CLAUDE.md`, session-continuity risk is a mandatory checkpoint trigger.

---

## Where to pick up

**Exact next action:** invoke `superpowers:subagent-driven-development` against the plan at `docs/superpowers/plans/2026-05-07-rrxray-phase-2.1c-revenue-motion.md`. Start with Task 1.

**Reading order for a fresh session:**

1. `CLAUDE.md` — project memory + mandatory checkpoint rule + workflow conventions
2. `docs/checkpoints/2026-05-07-phase-2.1b-section-a-multi-collector-checkpoint.md` — what shipped through Phase 2.1b (most recent completed phase)
3. **This file** (`2026-05-07-phase-2.1c-execution-pending-checkpoint.md`) — current state
4. `docs/superpowers/specs/2026-05-07-rrxray-phase-2.1c-revenue-motion-design.md` — Phase 2.1c design spec
5. `docs/superpowers/plans/2026-05-07-rrxray-phase-2.1c-revenue-motion.md` — Phase 2.1c implementation plan (12 TDD tasks)

After reading those five files, you have full context. Skip everything else.

---

## What's queued (Phase 2.1c contents)

`revenue_motion` collector — third Section A signal. Adds careers page scraping, ATS-link follow (Lever / Greenhouse / Ashby / Workable), LinkedIn job postings via search, LinkedIn employee count snippet, and rule-based findings emission. Plus extends `FirecrawlClient` with `search()` method (Phase 1 deferred capability).

12 TDD tasks in the plan:

1. **T1: FirecrawlClient.search() extension** — adds the SDK wrapper Phase 1 deferred (Phase 2.2 + 2.3 will reuse this)
2. **T2: RevenueMotionData + JobPosting schemas**
3. **T3: Add revenue_motion field to CollectorOutputs**
4. **T4: Role catalog (8 categories) + ATS pattern catalog (4 platforms)**
5. **T5: Careers page URL discovery + ATS link detection (collector skeleton)**
6. **T6: HTML role extraction with title categorization**
7. **T7: LinkedIn search integration (jobs + employee count snippet)**
8. **T8: Rule-based findings emission**
9. **T9: Evidence writing + full collect() orchestration**
10. **T10: Renderer Module Detail partial + integration**
11. **T11: Synthesizer prompt update + body update (third conditional block)**
12. **T12: Pipeline registration + Dale-led quality gate (3-domain smoke + iteration)**

Task 12's quality gate is intentionally bounded by Dale's sign-off, not by time. The implementer subagent must NOT mark Phase 2.1c complete on its own — it presents output and pauses for Dale's review.

Roughly 2-3 hours of subagent execution + the Dale-led iteration loop.

---

## Process notes for the next session

**Workflow that's been working:**
- `superpowers:brainstorming` → spec → user review → `superpowers:writing-plans` → plan → `superpowers:subagent-driven-development` → fresh subagent per task → live smoke + Dale review → checkpoint
- Subagent dispatches use `general-purpose` agent type (the `superpowers:code-reviewer` agent type was unavailable in some prior sessions; if it's loaded, use it for code-quality review)
- Skip formal spec-review and code-quality-review subagent dispatches on truly mechanical tasks (renames, schema additions, simple data files). For tasks with real logic, do both reviews. Phase 2.1b execution called this judgment correctly per task.
- Local verification step: after each implementer subagent reports DONE, run `uv run pytest -v 2>&1 | tail -3` locally to confirm test count + pass/fail before dispatching review subagents.

**Quality-gate loop (T12) lessons from Phase 2.1b:**
- First-pass output may read as "observation" rather than "diagnostic." Critique was: synthesizer enumerated plausible explanations and walked away rather than committing to a hypothesis.
- Phase 2.1b iteration cycle that fixed this is documented in the Phase 2.1b checkpoint. The same pattern likely applies here: first pass may need iteration, expect 1-2 cycles of prompt tuning before sign-off.
- LLM emits em dashes despite prompt instruction. The voice post-processor's `sanitize_llm_output()` method handles this (added during Phase 2.1b iteration). No new fix needed for Phase 2.1c.

---

## Environment quick-reference

- Working directory: `/Users/dalezwizinski/Documents/Apps/rrxray`
- Python: 3.14 in `.venv/`
- uv path: `/Users/dalezwizinski/Library/Python/3.9/bin/uv` (uv not on standard PATH; use full path)
- Pre-commit: empty `ANTHROPIC_API_KEY` in shell shadows `.env` — Phase 1 fix at `7f9403f` filters empty env vars at Config layer; no manual `unset` needed
- API keys: in `/Users/dalezwizinski/Documents/Apps/rrxray/.env` (gitignored)

To verify environment is healthy after picking up:

```bash
cd /Users/dalezwizinski/Documents/Apps/rrxray
/Users/dalezwizinski/Library/Python/3.9/bin/uv run pytest -v 2>&1 | tail -3
# Expected: 188 passed, 1 skipped
```

If that count is wrong, something has shifted from the recorded state.

---

## What's NOT in this checkpoint (because it's elsewhere)

- Architecture decisions for Phase 2.1c — see the design spec
- Code for each task — see the implementation plan (full code in every step)
- Brand voice rules — see `CLAUDE.md` and `rr_voice.py`
- Workflow conventions — see `CLAUDE.md`
- Phase 1-2.1b state — see prior checkpoints in `docs/checkpoints/`

This checkpoint is intentionally short. The plan and spec carry the detail.

---

## Pointers

- [`CLAUDE.md`](../../CLAUDE.md) — project memory + mandatory checkpoint rule
- [`roadmap.md`](../../roadmap.md) — phased roadmap with decisions log
- [Phase 2.1b checkpoint](2026-05-07-phase-2.1b-section-a-multi-collector-checkpoint.md) — most recent completed-phase checkpoint
- [Phase 2.1c design spec](../superpowers/specs/2026-05-07-rrxray-phase-2.1c-revenue-motion-design.md)
- [Phase 2.1c implementation plan](../superpowers/plans/2026-05-07-rrxray-phase-2.1c-revenue-motion.md)
